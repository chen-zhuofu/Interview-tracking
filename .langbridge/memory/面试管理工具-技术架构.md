# 面试管理工具 — 技术架构

使用 FastAPI + SQLite + Jinja2 模板 + 原生 JavaScript（看板拖拽用 SortableJS CDN）构建。启动方式：`python run.py` 自动初始化数据库、启动 uvicorn 并打开浏览器。所有数据存储在 `data/interview_tracker.db`。前端页面通过 extends base.html 共享深色侧边栏布局。
